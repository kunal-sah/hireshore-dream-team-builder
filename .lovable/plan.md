## Add Yona Meissner's photo

Replace the `/placeholder.svg` currently used for Yona Meissner with the uploaded photo.

### Steps
1. Copy `user-uploads://image-42.png` → `public/lovable-uploads/yona-meissner.png`
2. Update the `photo` field for Yona in:
   - `src/components/TestimonialsSection.tsx`
   - `src/components/FoundersWall.tsx`
